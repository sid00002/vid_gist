// eslint-disable-next-line @typescript-eslint/no-unused-vars
type CoinsType = {
  coins: number;
};

// eslint-disable-next-line @typescript-eslint/no-unused-vars
type AddUrlErrorType = {
  url?: string;
  user_id?: string;
};

// eslint-disable-next-line @typescript-eslint/no-unused-vars
type SummaryType = {
  id: string;
  user_id: number;
  title: string;
  url: string;
  response: string | null;
  created_at: Date;
};
